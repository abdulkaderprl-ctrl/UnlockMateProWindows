<UserControl x:Class="UnlockMatePro.Views.GooglePixelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <!-- Left Panel: Device Info & Reboots -->
        <Border Grid.Column="0" Style="{StaticResource FluentCard}" Margin="0,0,12,0" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Device Info &amp; Status" Style="{StaticResource HeaderTextBlock}" FontSize="20" Margin="0,0,0,16" />

                <!-- Info Display -->
                <Border Grid.Row="1" Background="{DynamicResource ControlBackgroundBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,16">
                    <TextBlock Text="{Binding DeviceInfo}" Foreground="{DynamicResource PrimaryTextBrush}" TextWrapping="Wrap" />
                </Border>
                
                <Button Grid.Row="2" Content="Read Device Info (Fastboot)" Command="{Binding ReadInfoCommand}" Style="{StaticResource PrimaryButton}" Height="36" VerticalAlignment="Top" />

                <!-- Reboot Options -->
                <StackPanel Grid.Row="3">
                    <TextBlock Text="Reboot Options" Style="{StaticResource SubHeaderTextBlock}" Margin="0,0,0,8" />
                    <UniformGrid Columns="2" Rows="2">
                        <Button Content="System" Command="{Binding RebootCommand}" CommandParameter="system" Style="{StaticResource FluentButton}" Margin="0,0,4,4" />
                        <Button Content="Recovery" Command="{Binding RebootCommand}" CommandParameter="recovery" Style="{StaticResource FluentButton}" Margin="4,0,0,4" />
                        <Button Content="Bootloader" Command="{Binding RebootCommand}" CommandParameter="bootloader" Style="{StaticResource FluentButton}" Margin="0,4,4,0" />
                        <Button Content="FastbootD" Command="{Binding RebootCommand}" CommandParameter="fastbootd" Style="{StaticResource FluentButton}" Margin="4,4,0,0" />
                    </UniformGrid>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Middle Panel: Partition Flasher -->
        <Border Grid.Column="1" Style="{StaticResource FluentCard}" Margin="0,0,12,0" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Pixel Image Flasher" Style="{StaticResource HeaderTextBlock}" FontSize="20" Margin="0,0,0,16" />

                <!-- Individual Partitions -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Flash Individual Partitions" Style="{StaticResource SubHeaderTextBlock}" Margin="0,0,0,12" />
                        
                        <UniformGrid Columns="2">
                            <Button Content="Flash boot.img" Command="{Binding FlashPartitionCommand}" CommandParameter="boot" Style="{StaticResource FluentButton}" Margin="0,0,8,8" />
                            <Button Content="Flash init_boot.img" Command="{Binding FlashPartitionCommand}" CommandParameter="init_boot" Style="{StaticResource FluentButton}" Margin="0,0,0,8" />
                            
                            <Button Content="Flash vendor_boot.img" Command="{Binding FlashPartitionCommand}" CommandParameter="vendor_boot" Style="{StaticResource FluentButton}" Margin="0,0,8,8" />
                            <Button Content="Flash vbmeta.img" Command="{Binding FlashPartitionCommand}" CommandParameter="vbmeta" Style="{StaticResource FluentButton}" Margin="0,0,0,8" />
                            
                            <Button Content="Flash dtbo.img" Command="{Binding FlashPartitionCommand}" CommandParameter="dtbo" Style="{StaticResource FluentButton}" Margin="0,0,8,8" />
                            <Button Content="Flash super.img" Command="{Binding FlashPartitionCommand}" CommandParameter="super" Style="{StaticResource FluentButton}" Margin="0,0,0,8" />
                            
                            <Button Content="Flash radio.img" Command="{Binding FlashPartitionCommand}" CommandParameter="radio" Style="{StaticResource FluentButton}" Margin="0,0,8,8" />
                            <Button Content="Flash bootloader.img" Command="{Binding FlashPartitionCommand}" CommandParameter="bootloader" Style="{StaticResource FluentButton}" Margin="0,0,0,8" />
                        </UniformGrid>
                    </StackPanel>
                </ScrollViewer>

                <!-- Factory Image Flasher -->
                <Border Grid.Row="2" BorderBrush="{DynamicResource ControlBorderBrush}" BorderThickness="0,1,0,0" Padding="0,16,0,0" Margin="0,12,0,0">
                    <StackPanel>
                        <TextBlock Text="Flash Factory Image (Update ZIP)" Style="{StaticResource SubHeaderTextBlock}" Margin="0,0,0,8" />
                        <TextBlock Text="Flashes standard Google factory image zip file and wipes data." Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,0,12" TextWrapping="Wrap" />
                        <Button Content="Select &amp; Flash Factory ZIP" Command="{Binding FlashFactoryImageCommand}" Style="{StaticResource DangerButton}" Height="40" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Right Panel: Bootloader Control -->
        <Border Grid.Column="2" Style="{StaticResource FluentCard}" Padding="12">
            <StackPanel>
                <TextBlock Text="Bootloader &amp; Security" Style="{StaticResource HeaderTextBlock}" FontSize="20" Margin="0,0,0,16" />
                
                <TextBlock Text="Bootloader Unlock" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,8" />
                <Button Content="Unlock Bootloader" Command="{Binding BootloaderUnlockCommand}" Style="{StaticResource DangerButton}" Height="36" Margin="0,0,0,8" />
                <Button Content="Relock Bootloader" Command="{Binding BootloaderRelockCommand}" Style="{StaticResource PrimaryButton}" Height="36" Margin="0,0,0,24" />

                <TextBlock Text="Warning: Unlocking or Relocking the bootloader will wipe all user data on the device!" 
                           Foreground="{DynamicResource DangerBrush}" TextWrapping="Wrap" FontSize="12" />
            </StackPanel>
        </Border>

    </Grid>
</UserControl>
