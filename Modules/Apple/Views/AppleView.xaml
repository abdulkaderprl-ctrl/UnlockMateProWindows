<UserControl x:Class="UnlockMatePro.Views.AppleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UnlockMatePro.Views"
             xmlns:vm="clr-namespace:UnlockMatePro.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="{DynamicResource BackgroundBrush}">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Apple Professional Manager" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                    <TextBlock Text="Manage Apple devices in Normal, Recovery, and DFU modes" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,2,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding DeviceStatusText}" Foreground="{DynamicResource AccentBrush}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="Detect Device" Command="{Binding DetectDeviceCommand}" Width="120" Height="36"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Grid -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Tools Section -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,10,0">
                <StackPanel>
                    
                    <!-- General Tools -->
                    <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="General Tools" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10"/>
                            
                            <WrapPanel>
                                <Button Style="{StaticResource FluentButton}" Content="Read Info" Command="{Binding ReadInfoCommand}" Width="130" Height="36" Margin="0,0,10,10"/>
                                <Button Style="{StaticResource FluentButton}" Content="Check Activation" Command="{Binding CheckActivationCommand}" Width="160" Height="36" Margin="0,0,10,10"/>
                                <Button Style="{StaticResource FluentButton}" Content="Find My iPhone Check" Command="{Binding CheckFindMyIphoneCommand}" Width="180" Height="36" Margin="0,0,10,10"/>
                            </WrapPanel>
                            
                            <TextBlock Text="Reboot Options" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5,0,5"/>
                            <WrapPanel>
                                <Button Style="{StaticResource FluentButton}" Content="Reboot Device" Command="{Binding RebootDeviceCommand}" Width="130" Height="32" Margin="0,0,10,10"/>
                                <Button Style="{StaticResource FluentButton}" Content="Enter Recovery" Command="{Binding EnterRecoveryCommand}" Width="130" Height="32" Margin="0,0,10,10"/>
                                <Button Style="{StaticResource FluentButton}" Content="Exit Recovery" Command="{Binding ExitRecoveryCommand}" Width="130" Height="32" Margin="0,0,10,10"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <!-- Firmware Flasher -->
                    <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="Firmware Flasher (IPSW)" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,15"/>
                            
                            <!-- IPSW Path -->
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="IPSW File:" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBox Grid.Column="1" Style="{StaticResource FluentTextBox}" Text="{Binding IpswPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                                <Button Grid.Column="2" Style="{StaticResource FluentButton}" Content="..." Command="{Binding BrowseIpswCommand}" Width="40" Height="30"/>
                            </Grid>

                            <WrapPanel>
                                <Button Style="{StaticResource SuccessButton}" Content="Flash Firmware (Update)" Command="{Binding FlashIpswCommand}" Width="180" Height="36" Margin="0,0,10,0"/>
                                <Button Style="{StaticResource DangerButton}" Content="Restore Firmware (Erase)" Command="{Binding RestoreFirmwareCommand}" Width="180" Height="36"/>
                            </WrapPanel>

                        </StackPanel>
                    </Border>

                </StackPanel>
            </ScrollViewer>

            <!-- Logs Section -->
            <Border Grid.Column="1" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Operation Log" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10"/>
                    
                    <TextBlock Grid.Row="1" Text="{Binding StatusText}" Foreground="{DynamicResource AccentBrush}" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ProgressBar Grid.Row="2" Value="{Binding Progress}" Height="4" Margin="0,0,0,10" Background="{DynamicResource CardBorderBrush}" Foreground="{DynamicResource AccentBrush}" BorderThickness="0"/>
                    
                    <TextBox Grid.Row="3" Text="{Binding LogText}" Style="{StaticResource FluentTextBox}" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}" BorderThickness="0"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
