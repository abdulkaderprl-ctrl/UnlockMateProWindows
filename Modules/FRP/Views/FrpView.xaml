<UserControl x:Class="UnlockMatePro.Views.FrpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="350" />
        </Grid.ColumnDefinitions>

        <!-- Left Panel: Brands -->
        <Border Grid.Column="0" Style="{StaticResource FluentCard}" Margin="0,0,12,0" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="FRP Brands &amp; Modes" Style="{StaticResource HeaderTextBlock}" FontSize="20" Margin="0,0,0,12" />

                <!-- Brand List -->
                <ListBox Grid.Row="1" ItemsSource="{Binding SupportedBrands}" SelectedItem="{Binding BrandName}" 
                         Background="Transparent" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontSize="14" Margin="4" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <!-- Middle Panel: Tool Cards & Action Buttons -->
        <Border Grid.Column="1" Style="{StaticResource FluentCard}" Margin="0,0,12,0" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="{Binding BrandName, StringFormat='{}Selected: {0} FRP Operations'}" Style="{StaticResource SubHeaderTextBlock}" VerticalAlignment="Center" />
                </StackPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        
                        <TextBlock Text="MTP Mode Operations" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,8"/>
                        <WrapPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <Button Style="{StaticResource PrimaryButton}" Content="Enable ADB (Test Mode)" Width="170" Height="40" Margin="0,0,8,8" Command="{Binding SamsungMtpCommand}" />
                            <Button Style="{StaticResource FluentButton}" Content="Open Browser (YouTube)" Width="170" Height="40" Margin="0,0,8,8" />
                            <Button Style="{StaticResource FluentButton}" Content="Open Browser (Maps)" Width="170" Height="40" Margin="0,0,8,8" />
                        </WrapPanel>

                        <TextBlock Text="ADB Mode Operations" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,8"/>
                        <WrapPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <Button Style="{StaticResource FluentButton}" Foreground="{DynamicResource ErrorBrush}" Content="Bypass FRP (ADB)" Width="170" Height="40" Margin="0,0,8,8" Command="{Binding BypassAdbCommand}" />
                            <Button Style="{StaticResource FluentButton}" Content="Factory Reset (ADB)" Width="170" Height="40" Margin="0,0,8,8" />
                        </WrapPanel>

                        <TextBlock Text="Fastboot Mode Operations" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,8"/>
                        <WrapPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <Button Style="{StaticResource FluentButton}" Foreground="{DynamicResource ErrorBrush}" Content="Erase FRP (Fastboot)" Width="170" Height="40" Margin="0,0,8,8" Command="{Binding EraseFastbootCommand}" />
                            <Button Style="{StaticResource FluentButton}" Content="Factory Reset (Fastboot)" Width="170" Height="40" Margin="0,0,8,8" />
                        </WrapPanel>

                    </StackPanel>
                </ScrollViewer>

                <!-- Information placeholder -->
                <TextBlock Grid.Row="2" Text="Select a brand and operation..." Foreground="{DynamicResource MutedTextBrush}" FontStyle="Italic" VerticalAlignment="Center" HorizontalAlignment="Center" />
            </Grid>
        </Border>

        <!-- Right Panel: Device Info & Live Logs -->
        <Border Grid.Column="2" Style="{StaticResource FluentCard}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Module Status & Device -->
                <StackPanel Grid.Row="0" Margin="12,12,12,8">
                    <TextBlock Text="Device &amp; Status" Style="{StaticResource SubHeaderTextBlock}" />
                    <Border Background="{DynamicResource ControlBackgroundBrush}" CornerRadius="4" Padding="8" Margin="0,4,0,0">
                        <StackPanel>
                            <TextBlock Text="{Binding DeviceStatusText}" Foreground="{DynamicResource SuccessBrush}" FontWeight="SemiBold" />
                            <Button Style="{StaticResource FluentButton}" Content="Detect Device" Margin="0,8,0,0" Command="{Binding DetectDeviceCommand}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Live Logs -->
                <Border Grid.Row="1" Background="{DynamicResource ConsoleBackgroundBrush}" Margin="12,0,12,12" CornerRadius="4" Padding="8">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBlock FontFamily="Consolas" Foreground="{DynamicResource ConsoleForegroundBrush}" FontSize="12" Text="{Binding FrpLog}" />
                    </ScrollViewer>
                </Border>

                <!-- Progress Panel & Status Bar -->
                <StackPanel Grid.Row="2" Margin="12,0,12,12">
                    <Grid Margin="0,0,0,4">
                        <TextBlock Text="{Binding StatusText}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" HorizontalAlignment="Left" />
                        <TextBlock Text="{Binding Progress, StringFormat='{}{0}%'}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" HorizontalAlignment="Right" />
                    </Grid>
                    <ProgressBar Style="{StaticResource FluentProgressBar}" Value="{Binding Progress}" Maximum="100" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
