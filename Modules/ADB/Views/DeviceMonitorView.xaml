<UserControl x:Class="UnlockMatePro.Views.DeviceMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">
            <Border Style="{StaticResource FluentCard}" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="Live System Monitor" Style="{StaticResource SubHeaderTextBlock}" />
                    <TextBlock Text="Real-time monitoring of Battery, RAM, and Storage." Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,0,12" />

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <CheckBox Content="Auto Refresh (5s)" IsChecked="{Binding IsAutoRefreshEnabled}" Margin="0,0,12,0" />
                        <Button Style="{StaticResource FluentButton}" Command="{Binding RefreshCommand}">
                            <TextBlock Text="Refresh Now" FontSize="12" />
                        </Button>
                    </StackPanel>

                    <!-- Battery -->
                    <TextBlock Text="Battery Level" FontWeight="SemiBold" Margin="0,8,0,4" />
                    <ProgressBar Minimum="0" Maximum="100" Value="{Binding Stats.BatteryLevel, FallbackValue=0}" Height="20" Margin="0,0,0,4" />
                    <TextBlock Text="{Binding Stats.BatteryLevel, StringFormat={}{0}%}" HorizontalAlignment="Right" FontSize="12" />

                    <!-- Storage -->
                    <TextBlock Text="Internal Storage" FontWeight="SemiBold" Margin="0,8,0,4" />
                    <ProgressBar Minimum="0" Maximum="{Binding Storage.TotalBytes, FallbackValue=100}" Value="{Binding Storage.UsedBytes, FallbackValue=0}" Height="20" Margin="0,0,0,4" />
                    <TextBlock Text="{Binding Storage.FreeBytes, StringFormat={}Free: {0} bytes}" HorizontalAlignment="Right" FontSize="12" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
