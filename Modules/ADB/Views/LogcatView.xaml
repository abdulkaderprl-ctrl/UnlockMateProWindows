<UserControl x:Class="UnlockMatePro.Views.LogcatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Style="{StaticResource PrimaryButton}" Command="{Binding StartLogcatCommand}" Margin="0,0,8,0">
                <TextBlock Text="Fetch Logcat" />
            </Button>
            <Button Style="{StaticResource FluentButton}" Command="{Binding ClearLogCommand}" Margin="0,0,8,0">
                <TextBlock Text="Clear" />
            </Button>
            <Button Style="{StaticResource FluentButton}" Command="{Binding SaveLogCommand}">
                <TextBlock Text="Save Log" />
            </Button>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource FluentCard}" Padding="0">
            <TextBox Text="{Binding LogOutput, Mode=OneWay}" 
                     IsReadOnly="True" 
                     TextWrapping="NoWrap" 
                     AcceptsReturn="True" 
                     VerticalScrollBarVisibility="Auto" 
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     Background="{DynamicResource TerminalBackgroundBrush}"
                     Foreground="{DynamicResource TerminalForegroundBrush}"
                     BorderThickness="0"
                     Padding="10" />
        </Border>
    </Grid>
</UserControl>
