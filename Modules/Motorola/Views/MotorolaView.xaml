<UserControl x:Class="UnlockMatePro.Views.MotorolaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UnlockMatePro.Views"
             xmlns:vm="clr-namespace:UnlockMatePro.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="{DynamicResource BackgroundBrush}">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Motorola Professional Manager" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                    <TextBlock Text="Manage Motorola devices in ADB, Fastboot, and Recovery modes" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,2,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding DeviceStatusText}" Foreground="{DynamicResource AccentBrush}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="Detect Device" Command="{Binding DetectDeviceCommand}" Width="120" Height="36"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Grid -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Tools Section -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,10,0">
                <StackPanel>
                    
                    <!-- General Tools -->
                    <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="General Tools" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10"/>
                            
                            <WrapPanel>
                                <Button Style="{StaticResource FluentButton}" Content="Read Info" Command="{Binding ReadInfoCommand}" Width="130" Height="36" Margin="0,0,10,10"/>
                                <Button Style="{StaticResource FluentButton}" Content="Wipe Data (Factory Reset)" Command="{Binding WipeDataCommand}" Width="180" Height="36" Margin="0,0,10,10"/>
                            </WrapPanel>
                            
                            <TextBlock Text="Reboot Options" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5,0,5"/>
                            <WrapPanel>
                                <Button Style="{StaticResource FluentButton}" Content="Reboot System" Command="{Binding RebootSystemCommand}" Width="120" Height="32" Margin="0,0,10,10"/>
                                <Button Style="{StaticResource FluentButton}" Content="Reboot Recovery" Command="{Binding RebootRecoveryCommand}" Width="130" Height="32" Margin="0,0,10,10"/>
                                <Button Style="{StaticResource FluentButton}" Content="Reboot Fastboot" Command="{Binding RebootFastbootCommand}" Width="130" Height="32" Margin="0,0,10,10"/>
                                <Button Style="{StaticResource FluentButton}" Content="Reboot EDL" Command="{Binding RebootEdlCommand}" Width="120" Height="32" Margin="0,0,10,10"/>
                            </WrapPanel>

                            <TextBlock Text="Bootloader Tools" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5,0,5"/>
                            <WrapPanel>
                                <Button Style="{StaticResource FluentButton}" Content="Unlock Bootloader" Command="{Binding UnlockBootloaderCommand}" Width="150" Height="32" Margin="0,0,10,10"/>
                                <Button Style="{StaticResource FluentButton}" Content="Relock Bootloader" Command="{Binding RelockBootloaderCommand}" Width="150" Height="32" Margin="0,0,10,10"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <!-- Fastboot Flasher -->
                    <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="Fastboot Flasher" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,15"/>
                            
                            <!-- Boot Image -->
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Boot Image:" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBox Grid.Column="1" Style="{StaticResource FluentTextBox}" Text="{Binding BootPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                                <Button Grid.Column="2" Style="{StaticResource FluentButton}" Content="..." Command="{Binding BrowseBootCommand}" Width="40" Height="30" Margin="0,0,10,0"/>
                                <Button Grid.Column="3" Style="{StaticResource SuccessButton}" Content="Flash Boot" Command="{Binding FlashBootCommand}" Width="110" Height="30"/>
                            </Grid>

                            <!-- Recovery Image -->
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Recovery:" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBox Grid.Column="1" Style="{StaticResource FluentTextBox}" Text="{Binding RecoveryPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                                <Button Grid.Column="2" Style="{StaticResource FluentButton}" Content="..." Command="{Binding BrowseRecoveryCommand}" Width="40" Height="30" Margin="0,0,10,0"/>
                                <Button Grid.Column="3" Style="{StaticResource SuccessButton}" Content="Flash Recovery" Command="{Binding FlashRecoveryCommand}" Width="110" Height="30"/>
                            </Grid>

                            <!-- VBMeta Image -->
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="VBMeta:" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBox Grid.Column="1" Style="{StaticResource FluentTextBox}" Text="{Binding VbmetaPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                                <Button Grid.Column="2" Style="{StaticResource FluentButton}" Content="..." Command="{Binding BrowseVbmetaCommand}" Width="40" Height="30" Margin="0,0,10,0"/>
                                <Button Grid.Column="3" Style="{StaticResource SuccessButton}" Content="Flash VBMeta" Command="{Binding FlashVbmetaCommand}" Width="110" Height="30"/>
                            </Grid>

                            <!-- Super Image -->
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Super Image:" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBox Grid.Column="1" Style="{StaticResource FluentTextBox}" Text="{Binding SuperPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                                <Button Grid.Column="2" Style="{StaticResource FluentButton}" Content="..." Command="{Binding BrowseSuperCommand}" Width="40" Height="30" Margin="0,0,10,0"/>
                                <Button Grid.Column="3" Style="{StaticResource SuccessButton}" Content="Flash Super" Command="{Binding FlashSuperCommand}" Width="110" Height="30"/>
                            </Grid>

                            <!-- Persist Image -->
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Persist Image:" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBox Grid.Column="1" Style="{StaticResource FluentTextBox}" Text="{Binding PersistPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                                <Button Grid.Column="2" Style="{StaticResource FluentButton}" Content="..." Command="{Binding BrowsePersistCommand}" Width="40" Height="30" Margin="0,0,10,0"/>
                                <Button Grid.Column="3" Style="{StaticResource SuccessButton}" Content="Flash Persist" Command="{Binding FlashPersistCommand}" Width="110" Height="30"/>
                            </Grid>

                            <Separator Background="{DynamicResource CardBorderBrush}" Margin="0,10,0,10"/>

                            <!-- UPDATE.APP / Firmware Flash -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="OFP Firmware:" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBox Grid.Column="1" Style="{StaticResource FluentTextBox}" Text="{Binding FirmwarePath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0" ToolTip="Select \.zip, .tar, script"/>
                                <Button Grid.Column="2" Style="{StaticResource FluentButton}" Content="..." Command="{Binding BrowseFirmwareCommand}" Width="40" Height="30" Margin="0,0,10,0"/>
                                <Button Grid.Column="3" Style="{StaticResource SuccessButton}" Content="Flash ROM" Command="{Binding FlashFirmwareCommand}" Width="110" Height="30"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Logs & Status Panel -->
            <Border Grid.Column="1" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Operation Logs" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10"/>
                    
                    <TextBox Grid.Row="1" Text="{Binding LogText, Mode=OneWay}" 
                             Style="{StaticResource FluentTextBox}" 
                             IsReadOnly="True" 
                             TextWrapping="Wrap" 
                             VerticalScrollBarVisibility="Auto" 
                             FontFamily="Consolas" 
                             Background="{DynamicResource TerminalBackgroundBrush}"
                             Foreground="{DynamicResource TerminalForegroundBrush}"
                             Margin="0,0,0,10"/>

                    <StackPanel Grid.Row="2">
                        <TextBlock Text="{Binding StatusText}" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,5"/>
                        <ProgressBar Value="{Binding Progress}" Height="4" BorderThickness="0" Background="{DynamicResource ControlBackgroundBrush}" Foreground="{DynamicResource AccentBrush}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
