<UserControl x:Class="UnlockMatePro.Views.SamsungView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <!-- Left Panel: Flasher / Firmware -->
        <Border Grid.Column="0" Style="{StaticResource FluentCard}" Margin="0,0,12,0" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Firmware Flasher (Odin)" Style="{StaticResource HeaderTextBlock}" FontSize="20" Margin="0,0,0,16" />

                <StackPanel Grid.Row="1">
                    <!-- AP -->
                    <TextBlock Text="AP (PDA)" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4"/>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Style="{StaticResource FluentTextBox}" Text="{Binding ApPath}" Margin="0,0,8,0" IsReadOnly="True" />
                        <Button Grid.Column="1" Style="{StaticResource FluentButton}" Content="..." Width="40" Command="{Binding BrowseApCommand}" />
                    </Grid>

                    <!-- BL -->
                    <TextBlock Text="BL (Bootloader)" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4"/>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Style="{StaticResource FluentTextBox}" Text="{Binding BlPath}" Margin="0,0,8,0" IsReadOnly="True" />
                        <Button Grid.Column="1" Style="{StaticResource FluentButton}" Content="..." Width="40" Command="{Binding BrowseBlCommand}" />
                    </Grid>

                    <!-- CP -->
                    <TextBlock Text="CP (Modem)" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4"/>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Style="{StaticResource FluentTextBox}" Text="{Binding CpPath}" Margin="0,0,8,0" IsReadOnly="True" />
                        <Button Grid.Column="1" Style="{StaticResource FluentButton}" Content="..." Width="40" Command="{Binding BrowseCpCommand}" />
                    </Grid>

                    <!-- CSC -->
                    <TextBlock Text="CSC / HOME_CSC" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4"/>
                    <Grid Margin="0,0,0,24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Style="{StaticResource FluentTextBox}" Text="{Binding CscPath}" Margin="0,0,8,0" IsReadOnly="True" />
                        <Button Grid.Column="1" Style="{StaticResource FluentButton}" Content="..." Width="40" Command="{Binding BrowseCscCommand}" />
                    </Grid>

                    <Button Style="{StaticResource DangerButton}" Content="Flash Firmware" Height="45" FontSize="14" FontWeight="Bold" Command="{Binding FlashFirmwareCommand}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Middle Panel: Tool Cards & Action Buttons -->
        <Border Grid.Column="1" Style="{StaticResource FluentCard}" Margin="0,0,12,0" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="Samsung Operations" Style="{StaticResource SubHeaderTextBlock}" VerticalAlignment="Center" />
                </StackPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        
                        <TextBlock Text="Download Mode Tools" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,8"/>
                        <WrapPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <Button Style="{StaticResource PrimaryButton}" Content="Read Info (MTP/DL)" Width="170" Height="40" Margin="0,0,8,8" Command="{Binding ReadInfoCommand}" />
                            <Button Style="{StaticResource PrimaryButton}" Content="Read PIT (DL)" Width="170" Height="40" Margin="0,0,8,8" Command="{Binding ReadPitCommand}" />
                            <Button Style="{StaticResource DangerButton}" Content="Reset FRP (DL)" Width="170" Height="40" Margin="0,0,8,8" Command="{Binding RemoveFrpCommand}" />
                        </WrapPanel>

                        <TextBlock Text="MTP Mode Tools" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,8"/>
                        <WrapPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <Button Style="{StaticResource FluentButton}" Content="Enable ADB (*#0*#)" Width="170" Height="40" Margin="0,0,8,8" />
                            <Button Style="{StaticResource FluentButton}" Content="Factory Reset (MTP)" Width="170" Height="40" Margin="0,0,8,8" />
                            <Button Style="{StaticResource FluentButton}" Content="Reboot Download" Width="170" Height="40" Margin="0,0,8,8" Command="{Binding RebootDownloadCommand}" />
                        </WrapPanel>

                        <TextBlock Text="ADB Mode Tools" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,8"/>
                        <WrapPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <Button Style="{StaticResource DangerButton}" Content="Reset FRP (ADB)" Width="170" Height="40" Margin="0,0,8,8" Command="{Binding RemoveFrpCommand}" />
                            <Button Style="{StaticResource FluentButton}" Content="Reboot Download" Width="170" Height="40" Margin="0,0,8,8" Command="{Binding RebootDownloadCommand}" />
                            <Button Style="{StaticResource FluentButton}" Content="Reboot Recovery" Width="170" Height="40" Margin="0,0,8,8" Command="{Binding RebootRecoveryCommand}" />
                            <Button Style="{StaticResource FluentButton}" Content="Reboot System" Width="170" Height="40" Margin="0,0,8,8" Command="{Binding RebootSystemCommand}" />
                        </WrapPanel>

                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Right Panel: Device Info & Live Logs -->
        <Border Grid.Column="2" Style="{StaticResource FluentCard}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Module Status & Device -->
                <StackPanel Grid.Row="0" Margin="12,12,12,8">
                    <TextBlock Text="Device Status" Style="{StaticResource SubHeaderTextBlock}" />
                    <Border Background="{DynamicResource ControlBackgroundBrush}" CornerRadius="4" Padding="8" Margin="0,4,0,0">
                        <StackPanel>
                            <TextBlock Text="{Binding DeviceStatusText}" Foreground="{DynamicResource SuccessBrush}" FontWeight="SemiBold" TextWrapping="Wrap" />
                            <Button Style="{StaticResource FluentButton}" Content="Auto Detect" Margin="0,8,0,0" Command="{Binding DetectDeviceCommand}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Live Logs -->
                <Border Grid.Row="1" Background="{DynamicResource ConsoleBackgroundBrush}" Margin="12,0,12,12" CornerRadius="4" Padding="8">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBlock FontFamily="Consolas" Foreground="{DynamicResource ConsoleForegroundBrush}" FontSize="12" Text="{Binding LogText}" TextWrapping="Wrap" />
                    </ScrollViewer>
                </Border>

                <!-- Progress Panel & Status Bar -->
                <StackPanel Grid.Row="2" Margin="12,0,12,12">
                    <Grid Margin="0,0,0,4">
                        <TextBlock Text="{Binding StatusText}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" HorizontalAlignment="Left" />
                        <TextBlock Text="{Binding Progress, StringFormat='{}{0}%'}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" HorizontalAlignment="Right" />
                    </Grid>
                    <ProgressBar Style="{StaticResource FluentProgressBar}" Value="{Binding Progress}" Maximum="100" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
