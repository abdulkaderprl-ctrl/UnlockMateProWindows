<UserControl x:Class="AdbEasyInstaller.Views.FileExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AllowDrop="True" Drop="UserControl_Drop" DragOver="UserControl_DragOver">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header Title -->
        <TextBlock Grid.Row="0" Text="Android Device File Explorer" Style="{StaticResource HeaderTextBlock}" />

        <!-- Toolbar & Address Breadcrumb Bar -->
        <Border Grid.Row="1" Style="{StaticResource FluentCard}" Padding="12" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Navigation Controls (Back, Up, Refresh) -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,12,0">
                    <Button Style="{StaticResource FluentButton}" Command="{Binding NavigateBackCommand}" Width="36" Height="34" Padding="0" ToolTip="Back Directory">
                        <TextBlock Text="⬅" FontSize="14" />
                    </Button>
                    <Button Style="{StaticResource FluentButton}" Command="{Binding NavigateUpCommand}" Width="36" Height="34" Padding="0" Margin="6,0,0,0" ToolTip="Parent Directory (Up)">
                        <TextBlock Text="⬆" FontSize="14" />
                    </Button>
                    <Button Style="{StaticResource FluentButton}" Command="{Binding RefreshCommand}" Width="36" Height="34" Padding="0" Margin="6,0,0,0" ToolTip="Refresh Directory">
                        <Path Data="{StaticResource IconRefresh}" Fill="{DynamicResource PrimaryTextBrush}" Width="14" Height="14" Stretch="Uniform" />
                    </Button>
                </StackPanel>

                <!-- Address Bar -->
                <Grid Grid.Column="1" Margin="0,0,12,0">
                    <TextBox Text="{Binding CurrentPath, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FluentTextBox}" Height="34" VerticalContentAlignment="Center">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding GoToPathCommand}" />
                        </TextBox.InputBindings>
                    </TextBox>
                </Grid>

                <!-- Search Input -->
                <Grid Grid.Column="2" Margin="0,0,12,0">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FluentTextBox}" Height="34" VerticalContentAlignment="Center" Tag="Search files..." />
                </Grid>

                <!-- Quick Action Buttons -->
                <StackPanel Grid.Column="3" Orientation="Horizontal">
                    <Button Style="{StaticResource FluentButton}" Command="{Binding NewFolderCommand}" Margin="0,0,6,0" Padding="12,6">
                        <TextBlock Text="+ New Folder" FontSize="12" />
                    </Button>
                    <Button Style="{StaticResource PrimaryButton}" Command="{Binding UploadToDeviceCommand}" Padding="14,6">
                        <TextBlock Text="Upload File" FontSize="12" />
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Directory DataGrid -->
        <Border Grid.Row="2" Style="{StaticResource FluentCard}" Padding="0" Margin="0,0,0,12">
            <DataGrid ItemsSource="{Binding DirectoryItems}" SelectedItem="{Binding SelectedItem}"
                      AutoGenerateColumns="False" IsReadOnly="True" RowHeight="36" HeadersVisibility="Column"
                      MouseDoubleClick="DataGrid_MouseDoubleClick">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="📁 Open Folder / Download File" Command="{Binding OpenSelectedItemCommand}" />
                        <Separator />
                        <MenuItem Header="📋 Copy Item Path" Command="{Binding CopyItemCommand}" />
                        <MenuItem Header="✂️ Cut Item" Command="{Binding CutItemCommand}" />
                        <MenuItem Header="📌 Paste Clipboard Item" Command="{Binding PasteItemCommand}" />
                        <Separator />
                        <MenuItem Header="📥 Download to PC" Command="{Binding CopyToPcCommand}" />
                        <MenuItem Header="📤 Upload to Device" Command="{Binding UploadToDeviceCommand}" />
                        <Separator />
                        <MenuItem Header="✏️ Rename" Command="{Binding RenameItemCommand}" />
                        <MenuItem Header="🗑️ Delete" Command="{Binding DeleteItemCommand}" />
                        <Separator />
                        <MenuItem Header="+ New Folder" Command="{Binding NewFolderCommand}" />
                        <MenuItem Header="🔄 Refresh" Command="{Binding RefreshCommand}" />
                    </ContextMenu>
                </DataGrid.ContextMenu>

                <DataGrid.Columns>
                    <DataGridTextColumn Header=" " Binding="{Binding IconBadge}" Width="36" />
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" />
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="110" />
                    <DataGridTextColumn Header="Size" Binding="{Binding FormattedSize}" Width="100" />
                    <DataGridTextColumn Header="Permissions" Binding="{Binding Permissions}" Width="110" />
                    <DataGridTextColumn Header="Last Modified" Binding="{Binding FormattedDate}" Width="140" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Download Location & Active Transfer Status Bar -->
        <Border Grid.Row="3" Style="{StaticResource FluentCard}" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Text="Default PC Download Path: " Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" />
                        <TextBlock Text="{Binding DefaultDownloadFolder}" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="12" FontWeight="SemiBold" />
                        <Button Style="{StaticResource FluentButton}" Command="{Binding SelectDownloadFolderCommand}" Margin="10,0,0,0" Padding="6,2" Content="Change Folder" FontSize="11" />
                    </StackPanel>

                    <StackPanel Visibility="{Binding IsTransferring, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,6,0,0">
                        <Grid Margin="0,0,0,4">
                            <TextBlock Text="{Binding TransferStatusText}" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="12" FontWeight="Bold" HorizontalAlignment="Left" />
                            <TextBlock Text="{Binding RemainingTimeText}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" HorizontalAlignment="Right" />
                        </Grid>
                        <ProgressBar Value="{Binding TransferProgress}" Style="{StaticResource FluentProgressBar}" Margin="0,0,0,4" />
                        <TextBlock Text="{Binding TransferSpeedText}" Foreground="{DynamicResource AccentBrush}" FontSize="11" />
                    </StackPanel>
                </StackPanel>

                <Button Grid.Column="1" Style="{StaticResource FluentButton}" Command="{Binding CancelTransferCommand}"
                        Visibility="{Binding IsTransferring, Converter={StaticResource BooleanToVisibilityConverter}}"
                        VerticalAlignment="Center" Padding="12,6" Margin="12,0,0,0">
                    <TextBlock Text="Cancel Transfer" Foreground="{DynamicResource ErrorBrush}" FontSize="12" />
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
